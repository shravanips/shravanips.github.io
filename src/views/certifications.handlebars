<section class="section">
  <h1 class="page-title">Certifications</h1>

  {{#each certifications}}
    <div class="card">
      <div class="card__title">{{name}}</div>
      <div class="card__meta">{{issuer}} · {{date}}</div>
    </div>
  {{/each}}
</section>
