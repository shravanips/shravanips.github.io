<section class="home-hero">
  <div class="home-hero__inner">
    <h1 class="home-hero__title">{{home.titleLine}}</h1>
    <p class="home-hero__nickname">{{home.nickname}}</p>

    <h2 class="home-hero__headline">{{home.headline}}</h2>

    <p class="home-hero__lead">{{home.p1}}</p>

    <div class="home-hero__note">
      <p>
        {{home.researchPrefix}}
        <a href="{{home.advisorLink}}" target="_blank" rel="noopener noreferrer">
          {{home.advisorName}}
        </a>
        {{home.researchSuffix}}
      </p>
    </div>

    {{#if home.interestTitle}}
      <p class="home-section-label">{{home.interestTitle}}</p>
    {{/if}}

    {{#if home.interests}}
      <div class="home-focus">
        {{#each home.interests}}
          <span class="home-focus__pill">{{this}}</span>
        {{/each}}
      </div>
    {{/if}}

    <div class="home-hero__actions">
      <a href="/publications" class="btn btn--hero">View Research</a>
      <a href="/contact" class="btn btn--ghost">Let’s Connect</a>
    </div>

    <div class="home-hero__extra">
      <p>
        Outside of research, I love capturing moments using my Fujifilm camera. I usually walk around my neighborhood in Hoboken on Sundays, or head to nearby places to click street photographs. You can explore some of my work in the
        <a href="/media">Media section</a>
        or visit my photography page
        <a href="https://www.instagram.com/shravu.frames/" target="_blank" rel="noopener noreferrer">@shravu.frames</a>.
      </p>

      <p>
        I also enjoy trekking and reading books — small things that keep me curious, grounded, and creatively inspired.
      </p>
    </div>
  </div>

  <div class="home-bottom">
    <div class="home-bottom__left">© Shravani Sawant — {{year}}</div>

    <div class="home-bottom__center">
      <span>{{home.location}}</span>
    </div>

    <div class="home-bottom__right">
      {{#if social.linkedin}}
        <a class="icon-pill" href="{{social.linkedin}}" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">in</a>
      {{/if}}
      {{#if social.github}}
        <a class="icon-pill" href="{{social.github}}" target="_blank" rel="noopener noreferrer" aria-label="GitHub">gh</a>
      {{/if}}
      {{#if social.medium}}
        <a class="icon-pill" href="{{social.medium}}" target="_blank" rel="noopener noreferrer" aria-label="Medium">md</a>
      {{/if}}
    </div>
  </div>
</section>